{{- if and .Values.worker.enabled .Values.worker.execution.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.worker.execution.namespace }}
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
{{- end }}
